// src/ControlPanel.jsx


const ControlPanel = () => {
  return (
    <div className="control-panel">
      <button onClick={() => window.location.reload()}>Reset Array</button>
    </div>
  );
};

export default ControlPanel;
